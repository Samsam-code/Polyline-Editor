This app is a simple tool to create and manipulate polylines.

Run ``main.py`` to start and use your mouse to click on the buttons.

Some solvers of reconfigurations of polylines according to different setup are located in the ``solver`` folder.
In particular, we experimented with some heuristic for points in general configurations and tested them in ``solver\benchmark.py``.